import React from "react";

// = PropertyCard — sa Red Alert badge

interface VenueCardProps {
  // TODO: definiši props
}

export default function VenueCard({}: VenueCardProps) {
  return <div>VenueCard</div>;
}
