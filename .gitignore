# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js build output
.next/
out/

# Turborepo task cache
.turbo/

# Production build
build/

# Environment files
.env
.env*.local
.env.production

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# Uploads (if local)
public/uploads/
graphify-out/
.claude/

# Playwright output. These are failure artifacts (traces, screenshots, videos) —
# committing them bloats the repo with binaries and they were also being indexed
# into the Graphify graph, diluting the audit instrument with test noise.
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Local design prototypes (Claude Design exports, mockups, iOS/Android HTML prototypes).
# Kept out of the repo deliberately: they are large, regenerated wholesale on every
# design pass, and are not the source of truth for any shipped code.
design/
