import OwnerPronadji from "@/screens/owner/pronadji";

/** Owner-only, pushed from Poslovi. Not a tab — the bar is full at five slots. */
export default function PronadjiRoute() {
  return <OwnerPronadji />;
}
